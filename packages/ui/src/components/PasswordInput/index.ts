import PasswordInput from './index.vue';
import { withInstall } from '~/_utils';

export const LPasswordInput = withInstall(PasswordInput);
export default LPasswordInput;
export * from './PasswordInput.types';
